# SQL queries (Project 8 — CLV / VIP targeting)

Core queries required by ADY201m Project 8:

| File | Purpose |
|------|---------|
| `01_rfm_metrics.sql` | RFM metrics per customer |
| `07_rfm_segments.sql` | RFM quintile groups / segments |
| `03_top_customers_monetary.sql` | Top customers by Monetary value |
| `02_cohort_retention.sql` | Retention rate by cohort month |

Additional analytics: `04`–`06`.

Run via DuckDB in `notebooks/02_sql_analysis.ipynb` or the SQL runner in `src/`.
